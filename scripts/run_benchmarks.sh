#!/usr/bin/env bash
# =============================================================================
# run_benchmarks.sh  --  benchmark entry point (placeholder until M6)
#
# Benchmarks (microbenchmarks + Dhrystone) and CPI/IPC reporting are added at
# milestone M6, once the pipeline, M extension, branch predictor, and
# performance counters exist. This stub exists so the interface is stable and
# the repo layout is complete; it intentionally produces no numbers, because
# there is nothing measured yet.
# =============================================================================
set -euo pipefail
echo "run_benchmarks.sh: no benchmarks yet."
echo "Benchmarks and CPI reporting arrive at M6 (see docs/../README.md roadmap)."
echo "Until then, no performance numbers exist and none will be fabricated."
exit 0
